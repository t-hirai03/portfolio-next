import React from 'react';

import styles from './index.module.scss';

const Header: React.FC = (): JSX.Element => {
  return <header className={styles.header}>ヘッダー</header>;
};

export default Header;
